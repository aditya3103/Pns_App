<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pns">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/appicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.PnS">
        <activity android:name=".ChiDistri"></activity>
        <activity android:name=".tdistri" />
        <activity android:name=".gamma_distr" />
        <activity android:name=".BinomialDistribution" />
        <activity android:name=".sample" />
        <activity android:name=".NormalDistribution" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>